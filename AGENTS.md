# Cert Namo
Vue/Vite: vuejszin. Nest/TypeORM: nestjszin.
Certificate fields: partnerOneName, partnerTwoName, coupleDate, location, accentKey. Public route: /c/:publicId.
Dev: /vrcel/* -> localhost:4242/*; production -> Render. Keep vrcel unchanged: it is the user's nickname.
Use EN-US code and PT-BR UI. TypeORM synchronize is enabled; do not start it against a real DB without explicit approval.
